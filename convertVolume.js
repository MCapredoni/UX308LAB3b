// Calculate the **volume** of a triangular prism. Start with a variable that contains the **base** of the triangular prism, and another variable that references the **height** in meters, followed by another variable for **length**

function convertVolume(base){
    return base *0.5*3*3*3;
    }
    
export {convertVolume}